"""Domain 10 — Barcode (new).

Planned (tables Phase 1; behavior Stage 2c): BarcodeLabel, ScanEvent. Mobile
QR/DataMatrix scanning as a responsive mode inside the Warehouse + Production
portals; ScanEvent is the scan ledger driving receive/move/consume/dispatch.
"""
